<!-- Слушать не только localhost: иначе порт 8123 отвечает лишь внутри
     контейнера, а опрос идёт снаружи. Ключа командной строки для этого нет —
     сервер сам подхватывает config.d рядом с config.xml. -->
<clickhouse>
    <listen_host>0.0.0.0</listen_host>
</clickhouse>
